{
  "name": "ai-contest-hub",
  "version": "1.8.0",
  "private": true,
  "scripts": {
    "prepare:web": "node scripts/prepare-capacitor.mjs",
    "test:native-bridge": "node scripts/test-native-bridge.mjs",
    "android:icon": "node scripts/reconstruct-icons.mjs && java scripts/GenerateAndroidIcons.java",
    "android:init": "npm run prepare:web && npx cap add android && npm run android:icon",
    "android:sync": "npm run prepare:web && npx cap sync android && npm run android:icon",
    "android:open": "npx cap open android",
    "ios:init": "npm run prepare:web && npx cap add ios",
    "ios:sync": "npm run prepare:web && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  }
}
